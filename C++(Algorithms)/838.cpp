#include<iostream>
#include<vector>

using namespace std;

void write_vec(vector<vector<long long> >);
void set_vec(vector<vector<long long> >&);
long long go(vector<vector<long long> >&);

int main()  {
	int n;
	cin>>n;
	vector<long long> T(n);
	vector<vector<long long> > v;
	
	
	return 0;
	
}
long long go(vector<vector<long long> > &v) {
	
}
